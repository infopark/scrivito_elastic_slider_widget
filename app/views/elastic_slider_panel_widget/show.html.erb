<div class="item <%= widget.additional_classes(params) %>">
  <%= cms_image_tag widget, :image %>
  <div class="container">
    <div class="carousel-caption">
      <%= cms_tag :h1, widget, :headline %>
      <div class="content">
        <%= cms_tag :p, widget, :content %>
        <% unless widget.button.blank? %>
          <p>
            <%= link_to widget.button.title, cms_path(widget.button), class: "btn btn-lg btn-primary", role: "button" %>
          </p>
        <% end %>
      </div>
    </div>
  </div>
</div>
